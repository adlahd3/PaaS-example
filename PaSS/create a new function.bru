meta {
  name: create a new function
  type: http
  seq: 1
}

post {
  url: http://localhost:3000/api/v1/functions
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "name": "hello",
    "code": "module.exports = async (req) => ({ message: \"hello\" })"
  }
}

settings {
  encodeUrl: false
  timeout: 0
}
